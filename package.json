{
  "name": "rewear-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/client & npm run dev --workspace=packages/server",
    "install": "npm install --workspaces"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}